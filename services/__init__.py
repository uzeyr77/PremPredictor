"""
Application services package.

- `predictions` — Monte Carlo engine and backtests (core math).
- `prediction_service` — orchestration for Flask (`PredictionService`).
- `repository` — SQLite access.
- `validators` — request/scenario validation.
"""
